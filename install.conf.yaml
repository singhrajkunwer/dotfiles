- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#     ~/.tmux.conf: tmux.conf
      ~/.zshrc: 
      ~/.gitconfig: gitconfig

#- create:
#    - ~/Work
#    - ~/hey/you/guys

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
